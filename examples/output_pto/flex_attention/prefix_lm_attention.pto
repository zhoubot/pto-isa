// PTO Program: prefix_lm_attention
// Generated by PTO ISA Compiler

func @prefix_lm_attention(%Q_mem: !pto.memref<gm,...,f32>, %K_mem: !pto.memref<gm,...,f32>, %V_mem: !pto.memref<gm,...,f32>, %mask_mem: !pto.memref<gm,...,f32>, %output_mem: !pto.memref<gm,...,f32>) {
  // Tile Declarations
  %Q = alloc_tile : !pto.tile<8x8xf32>
  %K = alloc_tile : !pto.tile<8x8xf32>
  %V = alloc_tile : !pto.tile<8x8xf32>
  %scores = alloc_tile : !pto.tile<8x8xf32>
  %scaled = alloc_tile : !pto.tile<8x8xf32>
  %prefix_mask = alloc_tile : !pto.tile<8x8xf32>
  %masked_scores = alloc_tile : !pto.tile<8x8xf32>
  %row_sum = alloc_tile : !pto.tile<8x1xf32>
  %shifted = alloc_tile : !pto.tile<8x8xf32>
  %exp_scores = alloc_tile : !pto.tile<8x8xf32>
  %attn = alloc_tile : !pto.tile<8x8xf32>
  %output = alloc_tile : !pto.tile<8x8xf32>
  
  // Instructions
  %Q = tload %Q_mem[0, 0] : (!pto.memref<gm,...,f32>, index, index) -> !pto.tile<8x8xf32>
  %K = tload %K_mem[0, 0] : (!pto.memref<gm,...,f32>, index, index) -> !pto.tile<8x8xf32>
  %V = tload %V_mem[0, 0] : (!pto.memref<gm,...,f32>, index, index) -> !pto.tile<8x8xf32>
  %prefix_mask = tload %mask_mem[0, 0] : (!pto.memref<gm,...,f32>, index, index) -> !pto.tile<8x8xf32>
  %scores = tmatmul %Q, %K : (!pto.tile<8x8xf32>, !pto.tile<8x8xf32>) -> !pto.tile<8x8xf32>
  %scaled = tmuls %scores, %0.35355339059327373 : !pto.tile<8x8xf32>, f32
  %masked_scores = tadd %scaled, %prefix_mask : !pto.tile<8x8xf32>
  %row_sum = trowsum %masked_scores : !pto.tile<8x8xf32> -> !pto.tile<8x1xf32>
  %row_sum = tdivs %row_sum, %8.0 : !pto.tile<8x1xf32>, f32
  %shifted = trowexpandsub %masked_scores, %row_sum : !pto.tile<8x8xf32>, !pto.tile<8x1xf32> -> !pto.tile<8x8xf32>
  %exp_scores = texp %shifted : !pto.tile<8x8xf32>
  %row_sum = trowsum %exp_scores : !pto.tile<8x8xf32> -> !pto.tile<8x1xf32>
  %attn = trowexpanddiv %exp_scores, %row_sum : !pto.tile<8x8xf32>, !pto.tile<8x1xf32> -> !pto.tile<8x8xf32>
  %output = tmatmul %attn, %V : (!pto.tile<8x8xf32>, !pto.tile<8x8xf32>) -> !pto.tile<8x8xf32>
  tstore %output, %output_mem[0, 0]
  
  return
}