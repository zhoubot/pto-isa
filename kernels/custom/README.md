# kernels/custom/

Examples and scaffolding for custom kernel/operator implementations.

## Contents

- `gemm_python/`: Python-side custom GEMM example and related integration files.

Use this directory as the starting point when adding custom PTO-based kernels that are not part of the baseline manual kernels.
